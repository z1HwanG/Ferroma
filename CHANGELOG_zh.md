# 更新日志

Ferroma 的重要变更，新的在前。

每个已发布版本都是一个 `vX.Y.Z` 标签，其值必须与 `Cargo.toml` 一致——因为
`.github/workflows/docker-publish.yml` 会拒绝与清单不符的标签，标签因此无法标记一棵
它并未构建的源码树。1.0 之前，次版本号可能改变任何东西，修订号则只修缺陷。

章节形式大体沿用 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)：某个版本
没有内容的章节直接省略，不留空标题。英文原版见 [`CHANGELOG.md`](CHANGELOG.md)。

## [未发布]

## [0.1.6] — 2026-09-20

### 变更

- **初始化就是一个页面。** 没有数据库时控制台就在 `/`；数据库那一步把同一个页面交给首次运行向导，
  而不是把人送到另一个地址；填完之后回到域名根目录，而不是控制台自己的路径。

### 修复

- **域名 DNS 面板读的是 `nslookup`，其中两种应答格式没被认识。** TXT 应答是
  `name  text = "…"`（记录类型写的是 `text`，绝不是 `TXT`），于是记录发布得没错、面板上
  SPF/DKIM/DMARC 却全空；地址应答是 `Name:` 加 `Address:`，所以 A/AAAA 两行也没有值，而 PTR 那行
  根本无法运行——它的"期望地址"是一个从来没有被填过的字段，现在改为从该域自己发布的 A 记录得出，
  而那正是 PTR 必须回答的地址。被拆成多段引号的 DKIM 公钥现在会拼成一段，也就是运维要粘贴的那一串。

## [0.1.5] — 2026-09-20

### 变更

- **在有人做出选择之前，界面跟随系统的浅色/深色设置。** 主题默认值原本是浅色，现在是「跟随
  系统」，于是整天开着深色桌面的机器不会再收到一个白底的邮件客户端。这推翻了 0.1.1 里记下的那个
  决定——"因为某人笔记本是那么设的，界面就变全黑"——那是一个关于"没人做出的决定"的合理论证，而这里
  说的是首次访问：那正是系统设置成为最可靠证据的场合。切换按钮与选择器里的三个选项都不变，显式选择
  依然优先并被记住。

- **首次运行向导会自己把填好的设置应用上去。** hostname、公开 URL、HTTP 监听地址与 TLS 材料都是
  启动时读取一次（运行中的进程搬不动自己的套接字，也不会重读 PEM），所以向导把它们存下来，然后由
  服务器自己重启去采用——而不是打印一句"重启容器后生效"。它替换自己的进程镜像（`exec`，容器因此
  保留端口、卷与同一个 PID 1），控制台等它重新应答后自动进去；平台做不到时页面会明说，剩下的交给
  容器的重启策略。一个以"请你去把什么东西重启一下"结尾的向导，填它的人根本无法判断它到底成功没有。

- **初始化只在一个地址上完成：`/`。** 只要实例还欠一个管理员——没有数据库，或者库里没有管理员——
  `/` 就提供 Admin 控制台，由控制台显示当前还差哪一步：先是问连接地址与一次性 code 的表单，然后是
  首次运行向导。在此之前，数据库那一步在 `/admin/`，而数据库一旦被接受，尚未初始化完的实例就把
  `/` 让给了 Webmail：它的登录框此时根本不能用（它自己的登录请求会被回答 `405`），于是运维打开邮件
  域名看到的是一张看起来坏掉的页面，而还欠的那一步就在旁边一个路径上——初始化被拆成了两个地址。
  `/admin/` 全程提供同一个控制台（更早的版本打印的就是这个地址），`/shared` 也没挪动，所以这只是
  一个应用挂了两次；一旦管理员存在，`/` 就是 Webmail——邮件域名本来就该是它。
  `crates/ferroma-api/src/router.rs` 现在把这个选择写成显式的 `RootApp`，并说明为什么挂在 `/`
  的应用必须是那个目录本身被挂在这里的应用；控制台自己的页面结束时改为跳转到 `/admin/`，而不是
  重载 `/`。

### 修复

- **"前端没有启动"的那个报错面板，现在会说所选语言。** `shared/diag.js` 原本把文案写成英文是
  有意的——它是加载在应用之前的经典脚本，读不到语言词条表——结果是设成中文的控制台把自己最严重的
  故障用英文报出来，而那恰恰是运维最看不懂的一条消息。它现在带一张两词条表，并采用与词条表相同的
  规则（存下来的选择优先，默认英文），同时保持零依赖。

- **Webmail 不再保留一个指向另一个应用的地址。** 哈希是两个前端各自表达"屏幕上是什么"的方式，而
  形状不同：Webmail 用 `#/f/<文件夹>` 与 `#/search/…`，控制台用挂在 `/admin/` 下的 `#/<section>`。
  把两者混起来的 URL——`/#/admin/`，产品不会生成它，但人会手输——会加载 Webmail，于是屏幕上显示
  收件箱，而地址栏说的是控制台：`web/router.js` 开头那句承诺正好相反。现在无法识别的哈希会被替换成
  真正正在显示的那条路由（写回地址栏）。

- **全新安装可能停在登录页，而不是首次运行向导。** 控制台靠 `GET /api/v1/setup` 决定显示哪一页，
  而在数据库地址被接受的瞬间——也就是页面重载的那一刻——进程还在从 bootstrap 路由切换到真正的
  API。这个窗口里的请求被前端的 SPA 兜底接走了：返回 Webmail 的 HTML 与 `200`。控制台把它报成
  「响应不是 JSON」，并理解成*没有向导*，于是运维看到的是一张声称"管理员已存在"的登录框，而那个
  实例刚刚才被告知要创建第一个管理员；只有硬刷新才能过这一步——这不是能要求运维去做的事。
  bootstrap 路由现在把每个未知的 API 路径都按运行中服务器的同一种 JSON `404` 回答，API 请求因此
  不可能收到一个页面；控制台的两个启动探测也会对"无法解析的 200"重试，而不是把它当成一个决定。

- **镜像里有六个静态文件服务用户读不到，导致 Webmail 与 Admin 白屏。** `COPY` 会原样保留被
  复制文件的权限位，而检出里的源文件完全可能处于 `0600`——某些编辑器与 agent 工具就这么写文件，
  而 git 只记录可执行位，代码评审里看不出任何异常。服务以 uid `10001` 运行，于是
  `shared/i18n.js`、`shared/diag.js`、`shared/locales/zh-CN.js`、`admin/icons.js`、
  `admin/views/bootstrap.js`、`admin/views/first-run.js` 经静态文件服务一律返回 **404**；
  前端的 ES 模块图加载失败，页面上什么都没有，而服务端日志里没有任何东西能把"白屏"和"权限位"
  联系起来。`Dockerfile` 现在统一规整它发布的内容（`chmod -R a+rX /usr/share/ferroma
  /etc/ferroma`），不再信任构建机器的 umask；`tools/check-deploy.mjs` 也会因为静态文件不可被
  所有人读取而失败——bind mount 一份检出的部署拿不到 Dockerfile 的保护。0.1.3 带有同一缺陷。

## [0.1.4] — 2026-09-20

### 新增

- **自建文件夹可以拖动到别处了。** 把文件夹拖到另一个文件夹的**中间**就是移入其中——这是真正的移动：
  `PATCH /folders/:id` 现在接受 `parent_id`（`null` 表示顶层），服务器会同时改写该文件夹与所有子文件夹
  的路径、搬移对应的 Maildir 目录，并拒绝会形成「文件夹放进自己内部」的移动。拖到某一行的**边缘**仍是
  原来的排序，拖到树的**空白处**则把文件夹提到顶层。此前手势是按目标行的父目录判断的，于是同一层级的
  两个文件夹永远只能互换位置——把它们相互嵌套在界面上根本做不到。


- **没有数据库的服务器现在会提供「询问数据库」的那个页面。** 此前它以
  `cannot connect to PostgreSQL` 直接退出——没有页面、没有控制台、无处可点。现在它照样绑定 web
  端口，把一次性设置码打印到日志，并只提供一个页面：填数据库地址与那个设置码。提交后先测试连接、应用
  表结构、把地址写入 `<data_dir>/database.json`（0600），随后**在同一个进程、同一个端口继续启动**
  ——不需要重启——页面刷新后直接进入首次运行向导。数据库必须已经存在，Ferroma 绝不执行
  `CREATE DATABASE`。**不写** `DATABASE_URL` 就会走这套流程；一旦显式声明就与其他设置一样以部署方
  为准——而且**显式声明**的地址连不上仍然是启动阶段的响亮错误，**已记住**的地址连不上则把页面带回来
  并说明原因，打错的密码可以在浏览器里就地改正。


- **首次运行向导就是安装的配置入口。** 它收集过去要通过参数或环境变量传入的东西——邮件域名、
  服务器主机名、公开 URL、API 监听地址与端口，以及 TLS（开关、证书、私钥）——并写入数据库；服务器
  在下次启动时采用那些部署方未显式声明的项。`GET /api/v1/setup`会返回上述全部字段的当前值供表单
  预填，`POST /api/v1/setup`则原样回答它实际存下了什么。
- **`scripts/deploy.sh --wizard`只问一个 web 端口。** 邮件域名、MX 主机名、首位管理员与 TLS 都在
  堆栈启动后于向导页面填写。该模式下脚本不写 `FERROMA_HOSTNAME`、`FERROMA_PUBLIC_URL`、
  `FERROMA_JWT_SECRET`——写了就会让向导的答案变成空操作——并且不再自动创建管理员，而是直接打印
  需要打开的地址。
- **随仓库提供的 compose 文件会发布 web 端口。** `docker-compose.prod.yml`现在映射
  `${WEB_PORT:-8080}:8080`，且它与`docker-compose.external-db.yml`都不再强制要求主机名或公开
  URL。全新安装只需要 web 端口（外加`deploy.sh`自动生成的数据库密码）；邮件端口保持固定默认值，
  因为容器一旦创建，Docker 就无法再追加发布的端口。


- **已保存的草稿可以继续编辑。** 点击「草稿箱」里的邮件会打开写信窗口，收件人、主题、正文与附件
  列表都已填好，标题为「编辑草稿」；保存走 `PATCH /drafts/:id`，发送走
  `POST /drafts/:id/send`，因此不会再留下一份「已发送邮件」的副本。此前它会在阅读窗格里打开，
  只能看到半成品正文却无法继续写。
- **自建文件夹保持用户给的顺序。** 默认按创建时间排序而不是按名称——新建的文件夹不会再插到树的
  中间；把文件夹拖到同级文件夹上即可调整顺序，顺序按地址分别记住。IMAP 本身没有文件夹顺序的概念，
  因此这项偏好和其他客户端偏好放在一起。
- **邮件里的链接会在新标签页打开。** 阅读窗格向 iframe 注入 `<base target="_blank">`，沙箱允许
  弹出窗口（仍然禁止脚本与同源访问），因此点击链接不会再用目标网页替换掉邮件、让人无路可退。


- **重命名文件夹会连同其下所有内容一起改名。** 把 `Projects` 改成 `Work` 时，子文件夹在数据库与
  Maildir 中的路径都会跟着改，与 IMAP `RENAME` 的语义一致。另外，当文件夹目录恰好缺失时不再返回
  令人费解的 `404`：数据库才是「有哪些文件夹」的依据，缺失的目录会被创建，而不是变成一个用户
  既看不见也无从处理的拒绝。
- **文件夹树显示自己的名字，而不是整条路径。** 嵌套文件夹的 IMAP 名是 `Projects/2026`，树里就
  原样打印出来，于是每个子项都在重复父项。现在只显示最后一段；`folder.name` 仍然保留完整路径，
  供所有需要发送它的操作使用。


- **首次运行向导无需登录即可打开，并成为全新安装的入口。** 它原先藏在 Admin 管理后台自己的
  登录面板之后，而在第一位管理员存在之前没人能通过那个面板——也就是说，用来创建管理员的
  向导永远打不开，装完之后只能靠手改环境变量收尾。现在 `GET /setup` 报 `{required:true}`
  时，Admin 管理后台会为未登录访客渲染 `#/setup` 且不再提供其他分区；Webmail 则把全新安装
  直接送到那里，而不是显示一个凭据根本不可能存在的登录面板。
- **向导接管主机名与公开 URL，服务端会采用它存下的值。** 提交与运行中配置不同的
  `server.hostname` 或 `api.public_url` 时，现在写入 `settings` 表而不是回 `400`，
  `server/src/serve.rs` 在启动时读回这两个值。优先级为 `ferroma.toml` / 环境变量 >
  数据库行 > 内置默认值：显式声明过主机名的部署永远不会被覆盖。`POST /setup` 用新增的
  `applied` 对象说明它存了什么。
- **未配置的 `api.jwt_secret` 会被生成并保留。** 此前它每次启动都随机生成后丢弃，导致每次
  重启都让所有会话失效，也让 `FERROMA_JWT_SECRET` 成为无法省略的启动项；现在会写入
  `<data_dir>/jwt_secret`（仅属主可读）并复用。显式配置的值仍然优先。
- **`POST /users` 会一并创建账号的主地址、Maildir 和标准文件夹**（只要该地址的域名已存在）
  ——这正是 `ferroma user create` 一直以来的行为。响应在 `mailboxes` 中返回创建的地址；空列表
  意味着域名不存在，控制台会如实说明而不是报告成功。此前在控制台创建的账号登录 Webmail 后既
  没有文件夹可打开、也没有地址可发信，什么功能都用不了。
- **嵌套文件夹真正可用。** `POST /mailboxes/:id/folders` 传入 `父级/子级` 名称时会创建缺失的
  父级，并为每一行记录 `parent_id`（IMAP `CREATE` 同样如此），Webmail 把树渲染为嵌套列表。
  名称一直就是完整的 IMAP 路径，只是控制台此前无法把它表达的层级画出来。

- **两个前端有了真正的设计系统。** 共享的 token（表面层级、阴影、圆角、时长/缓动刻度、
  焦点环）同时驱动两个应用；原先零散的润色（这里一个 hover、那里一个阴影）各自收敛成
  一份视觉层，`prefers-reduced-motion` 可以一次性全部关掉。
- **Webmail 的多选终于看得见，而且是常驻的。** 每行都带复选框，列表表头有带半选状态的
  “全选”，列表上方的操作栏永久占位——43px 高的图标按钮，未选中时只是禁用，不会出现或消失。
  Ctrl/Shift 点击与空格仍然可用——它们此前是多选的*唯一*途径，而一个看不见的控件算不上
  功能。
- **两个应用的主题切换都有动效。** Webmail 顶栏新增日/月切换按钮，Admin 的按钮重做，Webmail
  的“设置”里改成三段式分段控件，每个选项一个图标（跟随系统 / 浅色 / 深色）。切换主题时所有
  表面约 400ms 交叉淡入，图标在切换过程中旋转而不是闪一下。
- **标签页标题会写明当前视图。** Admin 此前每个分区都是 `Ferroma Admin`，Webmail 也始终是
  `Ferroma Webmail`：侧栏与文件夹窗格说明了你在哪里，唯独浏览器标签页——唯一能回答「十个
  标签页里哪个是哪个」的地方——没有。现在两边都是 `<分区> · <应用>`，已本地化，用的是
  控制台一直带着却没人用的 `SECTIONS[].title`。
- **图标只在有意义的地方出现。** Webmail 的阅读操作、文件夹工具栏、写信与搜索栏都配了图标；
  Admin 的统计卡片有图标徽章，可排序表头显示方向箭头，状态标签带一个色调圆点，表格行操作
  （详情 / 编辑 / 密码 / 启用 / 删除……）既有文字也有图形。

### 变更

- **两个界面默认使用英文与浅色。** 默认语言不再跟随 `navigator.languages`，默认主题也不再跟随
  `prefers-color-scheme`：一个实例通常有多个人访问，而因为某人笔记本的系统设置就让界面换语言或变成全黑，
  会让所有使用说明都失效。语言选择器（登录卡片与设置里各有一个）与深浅色切换都只有一次点击之遥，
  而「跟随系统」仍然是主题选项之一。
- **各页面标题下的一句说明改成了技术化表述。** 「Who did what, and when」改为
  「Administrative actions, with actor and timestamp」；「Mail domains hosted by this server」改为
  「Domains this server accepts and delivers mail for」；其余九条也一并统一了口径。


- **「初始化」不再是 Admin 侧栏的常驻项。** 已经完成初始化的实例侧栏里仍然挂着「初始化」，而那个页面只能回
  答「管理员已存在」。现在它只在尚无管理员时出现（向导就是通过它进入的），之后自动消失；直接访问
  `#/setup` 仍然可用，以照顾旧链接。


- **首次运行初始化现在是一个独立页面。** 没有侧栏、没有顶栏：初始化期间控制台只有一屏内容、也没有
  会话，于是它像登录卡片那样把外壳去掉。页面上自带首次使用者在此刻真正需要的两个控件——语言切换与
  深浅色切换——并且把说明文案缩短到不会再有一个词被挤到第二行。
- **`toggleTheme()` 翻转的是你能看到的配色。** 此前每个切换按钮翻转的都是`currentTheme()`，而它返回的
  是*模式*：从`auto`出发时第一次点击等于选择了显式的`dark`——当系统本来就是深色时，这一下看起来毫无
  反应。现在`resolvedTheme()`与`toggleTheme()`位于`shared/theme.js`，四个切换按钮统一使用。
- **DNS 结论与向导图标都来自图标集。** `admin/icons.js`补齐了向导切换按钮所需的`sun`/`moon`字形。


- **深浅色切换改为「光扫过整个窗口」，不再是交叉淡入。** 切换主题时用 View Transitions API 对页面
  取前后快照，并把即将生效的主题裁剪成一个从你点击的按钮扩散出去的圆——是「光来了」，而不是颜色
  悄悄互换。按钮同时会向外抛出一圈光环，日/月图标旋转的时长与揭示时长严格一致。不支持该 API 的
  浏览器仍走原来的交叉淡入；`prefers-reduced-motion` 下两者都跳过，切换就是瞬时的。


- **域名的 DNS 检查结果改为卡片，不再是五列表格。** DNS 记录不适合窄列：一条 SPF 或 DMARC 的值
  比承载它的单元格更长，于是抽屉里出现 `a:localho / st -all`、`p=quarant / ine` 这种断词，旁边还有
  横向滚动条。现在每条记录一张卡片——徽章、结论，然后是纵向排列且允许换行的预期值 / 实际值 / 提示。


- **加载骨架屏已移除。** 三个闪烁的方块代表尚不存在的邮件，在空文件夹里看起来就像三封空邮件。
  现在加载期间沿用该窗格自己的空状态文案。
- **点击已打开的文件夹会刷新它。** 哈希没变，路由事件不触发，因此什么都不会重新加载；现在第二次
  点击会重新读取文件夹树与邮件列表。
- **只有一个地址时它是标签，不是选择器。** 文件夹窗格里的发件地址选择器此前是一个禁用状态的
  `<select>`，显示被窗格截断的「地址（主地址）」，看起来像坏掉的控件，而不是「这些文件夹属于这个
  地址」。现在单地址时只是一行普通文字；账号拥有第二个地址的那一刻，它才作为控件回来。
- **登录卡片有了主题切换，顶部彩条也不再溢出卡片。** 彩条被卡片的圆角裁掉，日月切换按钮位于登录后
  顶栏按钮所在的那个角落。
- **提示对话框终于像个对话框。** `promptDialog` 原先是一个 560px 的卡片只放一个输入框，确认按钮还
  是危险样式；现在改为窄卡片：正规的标签、能说明答案形状的占位符、一句说明会发生什么，以及主色
  确认按钮——`danger: true` 只留给真正会删除东西的那两个提示。


- **Admin 侧栏高亮的就是你当前所在的分区。** 此前高亮只由 30 秒一次的健康轮询刷新，于是点击后
  的侧栏仍指着刚离开的分区，而右侧显示的却是你点开的那一个。
- **Webmail 顶栏不再重复「设置」。** 它自己有一个设置按钮，与头像菜单里的那一项重复；保留的是
  菜单里的条目，并且为管理员新增了**Admin 管理后台**入口。
- **登录卡片有了语言切换。** 此前唯一的语言入口在登录之后，对一个读不懂登录页的人毫无帮助。
- **深色模式下 HTML 邮件可读。** 邮件是按白纸排版的，深色客户端中间突然出现一大块白色，正是
  主题唯一明显失效的地方。阅读窗格负责拼装 iframe 的 `srcdoc`，因此在那里把正文反色——图片与
  视频再反一次，保持照片与标志的原始颜色。本身就以深色排版的邮件会变浅，这是所有反色客户端
  共同的取舍。


- **Webmail 左上角的按钮已移除。** 它只在 640px 以下打开文件夹抽屉——宽窗口从来不需要，
  而明确不在范围内的移动端现在也不再提供。文件夹窗格在任何宽度下都属于布局的一部分，
  它原来的位置现在放的是主题切换按钮。
- **打开邮件即刻标为已读。** 两秒延时以及随之而来的可见性判断都已删除：打开就是阅读，
  那个计时器只会让阅读窗格明明显示着的邮件在列表里保持未读。“设置”里关闭该行为的开关
  改名为“打开邮件时标为已读”。


- **Admin 侧栏始终固定展开。** 折叠按钮与窄屏抽屉都已移除：移动端不在考虑范围内，而一个会
  消失的导航只会让运维去找那个把它叫回来的按钮。
- **界面语言只在操作者确认后才切换。** 两个选择器原先都在 `change` 时直接刷新页面，方向键
  划过选项就会把整个界面换掉——Webmail 里发生在按下自己的保存按钮之前，Admin 管理后台里则
  完全没有确认步骤。Admin 的选择器新增了明确的 **应用** 按钮。
- **`GET /setup` 现在也返回 `public_url`**，向导因此可以用服务端实际对外声明的值预填两个
  身份字段。

### 移除

- **Docker 部署里的备份与恢复边车已全部移除。** 两个 compose 文件都不再定义 `backup` 或 `restore`
  服务，`ferroma-backups` 卷不复存在，`docker compose up -d` 现在只启动 Ferroma 本身。
  `scripts/backup.sh` 与 `scripts/restore.sh` 已删除，`scripts/deploy.sh` 不再有 `backup`、
  `restore` 子命令（`upgrade` 也不再先备份）。备份从此由运维自负，用宿主机自己的工具：导出数据库、
  复制 `ferroma-data` 卷（Maildir、附件与 DKIM 私钥都在其中），并让两者一起恢复——只恢复一半就会
  丢信。`docs/deployment.md` §8 列出了备份必须包含什么以及具体命令。这次也顺带去掉了额外的
  PostgreSQL 客户端镜像拉取，以及 `BACKUP_INTERVAL_SECONDS` / `BACKUP_RETENTION_DAYS` 两个设置。

- **一次发布只产出 `X.Y.Z` 与 `latest`，仅此而已。** 滚动的次版本 tag（`0.1`、`0.2`…）不再创建：
  它只是同一镜像的第二个名字，而一次补丁发布会悄悄改写运维可能已经 pin 住的名字——`:0.1` 会自己
  往前走。registry 层缓存也随之移除：它会把一个约 2 GB 的 `buildcache` tag 推进镜像仓库，每个运维
  都看得见、要计入该仓库的存储，却只惠及构建它的那台机器。缓存现在只在本地（`.cache/buildx`，
  已 gitignore），`--no-cache` 可以跳过。

### 修复

- **把文件夹拖到顶层行上时，客户端向服务器要的是「文件夹 0」。** `Number(null)` 等于 `0`，而读取父目录
  的辅助函数把它当成了父目录 id，于是拖到顶层行的**边缘**会发出 `parent_id: 0`，服务器回答
  `没有这个文件夹`——为一个确实存在的文件夹报出不存在的错。现在只有在 id 为正数时才算父目录，其余一律视
  为「没有父目录」；同时 API 对 `parent_id: 0` 直接给出解释性错误而不是 `404`——客户端 bug 不该看起来像
  数据丢失。
- **拖拽用自己的类型携带文件夹 id，且只认这个类型。** 当拖动从**选中文本**开始时，浏览器会把通用
  `text/plain` 载荷填成选中的文字，于是一个名为 `451` 的文件夹以字符串 "451" 出现、被当成文件夹 id，
  移动操作便回答 `没有这个文件夹`。现在 id 走 `text/x-ferroma-folder`，并会与屏幕上真实存在的文件夹
  核对；文件夹标签也不再是可选中的拖拽源。


- **「移到顶层」现在也是按钮，不再只能拖。** 把嵌套文件夹拖出去是能用的，但落点并不显眼；文件夹工具栏
  新增了一个显式动作（仅当该文件夹确实有父目录时可用），做的是同一件事。拖动时出现的落点也改为**任何**
  拖动都会显示，而不只是嵌套文件夹。
- **落下时从拖拽事件本身读取被拖的是哪个文件夹。** 之前 id 存在模块变量里，而 `dragend` 并非总会到达
  ——拖到窗口外、拖动被取消——残留的 id 会让**下一次**落下移动错误的文件夹。现在 id 随拖拽数据传递，
  变量仅作为兜底。


- **嵌套的子文件夹可以拖出来了。** 原先「移到顶层」的手势是「拖到树的空白处」——既不显眼，也不总有
  落点；现在拖动嵌套文件夹时，树末尾会出现一个虚线目标「**移到顶层**」。（这个目标刚写出来时本身也是坏
  的：它用模块里**严格**的 `byId` 去取自己，而该函数在元素不存在时会抛错——这个元素在开始拖动前恰恰
  不存在，异常直接把拖拽处理器打断了。）
- **前端资源现在带 `Cache-Control: no-cache`。** 它们是磁盘上就地修改的文件，而没有缓存头的浏览器可以
  启发式地保留它们——这就是为什么页面会一直跑着上周的模块、修好的 bug 看起来还在。现在每次请求都会
  重新校验（本机或局域网下就是一个 304），刷新一次即可。


- **文件夹的邮件计数现在由插入语句自己维护。** `messages.insert` 原先只递增 `uid_next`，保持
  `message_count` / `unseen_count` / `total_bytes` 正确是每个调用方的责任，而只有投递路径记得这件事。
  结果就是草稿箱的记录写着 `0`，里面却躺着 5 封草稿——表现为侧栏上某些文件夹没有计数，点进去却满屏
  邮件。现在计数与邮件行在同一个事务里一起变动，彻底删除时也会一起减回去。（第一次尝试把两个更新写
  进同一条语句的两个数据修改 CTE 里，而 PostgreSQL 只应用了其中一个——同一行在一条语句里只能被更新
  一次——计数看起来被维护了，实际仍然是错的；这也是它最终写成「一个事务、两条语句」的原因。）


- **存进数据库的证书路径不会再把服务器卡死在启动阶段。** 来自向导的 TLS 材料只有在文件确实存在时
  才会被采用——卷没挂上、路径打错、证书被轮换走——而配置里打不开的 PEM 会以一条醒目的错误回退到
  自签证书，而不是中止启动。拒绝启动会把用户锁在本来可以用来修路径的控制台之外；同时向导本身也不
  再会存下无效路径：它会检查文件在服务器上是否存在，并说明该路径由进程读取而非浏览器。
- **向导不再打印它所调用的接口**（`GET /api/v1/setup`），也不再在自己标题之上重复一层卡片标题。


- **阅读窗格只有一条滚动条，不是两条。** 之前窗格整体在滚动，而里面的正文 iframe 被写死
  `max(360px, 62vh)`，于是带附件的邮件把总高度顶出窗格：窗格长出一条滚动条，正文比框更长时框内
  又长出一条。现在头部与附件条是固定的（附件条上限 30vh，超出即在条内滚动），正文框拿走剩下的全部
  高度。短邮件带附件时**完全不出现滚动条**；长邮件只在框内滚动，长纯文本邮件只滚动正文区——绝不
  同时出现两条。


- **从抽屉里打开的对话框不再被抽屉遮住。** 抽屉 host 与对话框 host 都是 `<body>` 的子元素，且
  `z-index` 同为 60，于是后挂载的抽屉盖住了自己打开的确认框：「生成新的 DKIM 密钥？」这个问题被问在
  提问它那块面板的下面。现在对话框在抽屉之上，并且层叠顺序集中写明在一处。


- **服务端接受的域名现在也能在控制台里创建。** 新建域名对话框要求必须带点，而
  `ferroma_core::address::validate_domain` 是接受单标签的——于是 `demo`、`localhost` 或任何仅内部
  使用的域名根本无法从界面创建，只会得到一句「请输入有效的域名」。现在该对话框与首次运行向导共用
  同一个校验函数（与服务器规则一致），并在国际化域名需要用 punycode 形式时给出明确提示。


- **「添加地址」又能用了。** 该对话框调用了 `table(...)` 却没有从 `ui.js` 导入，于是打开就抛
  `table is not defined`，被 catch 当成表单错误显示出来，域名列表也一直是空的——这使得**在控制台里
  给账号添加第二个地址**根本不可能。（第一个地址不需要这个对话框：创建账号时就会一并建好。）
- **地址选择器只显示地址本身。** 追加「（主地址）」在 244px 的文件夹窗格里会被截断；账号抽屉里的
  地址表格本来就会标出主地址。


- **写信编辑器会显示列表标记了。** 全局 reset 去掉了所有 `ul`/`ol` 的标记，这对导航列表是对的，
  对邮件正文却是错的：项目符号行变成普通缩进文本，看起来就像工具栏按钮没生效。
- **沙箱检查断言错了对象。** 前端检查原先要求邮件 iframe 的 `sandbox` 属性必须字面为空，这与
  `docs/security.md` §10.2 矛盾——文档要求的值是 `allow-popups`，也正是它让链接能在新标签页打开。
  该规则现在断言沙箱必须*禁止*什么（脚本与同源访问）。


- **写信的格式按钮名副其实了。** 加粗/倾斜/下划线此前作用于浏览器当时剩下的选区——最坏情况是
  整封邮件；列表命令毫无效果；「清除」会留下仍然带项目符号的行；而链接按钮用的是
  `window.prompt`，那个原生弹窗无法美化，而且会把焦点还给一个选区已经丢失的页面状态，于是链接
  从未真正加上。现在每条命令都会先保存再恢复选区，编辑器起始就带一个块级元素让列表/引用有作用
  对象，「清除」也会一并去掉列表、引用与链接，链接改用应用自己的对话框。
- **空文件夹里不再显示三个空的骨架方块。** 加载骨架此前由加载函数内部开关，于是提前返回的调用
  ——比如上一页还在加载时又点了另一个文件夹——会让它一直闪在那里。
- **全局样式不再波及应用没有创建的标记。** `*` 滚动条规则、焦点环与主题过渡都限定在本应用自己的
  容器内；此前一个把面板注入页面的浏览器扩展会继承到它们。


- **邮件列表不再横向滚动。** 行摘要是内联 `<span>`，而 `text-overflow` 对内联盒子无效，
  于是很长的预览会把行撑到文本全宽，在窗格底部留下一条横向滚动条。
- **在两行之间移动选择会重绘这两行。** 列表的重绘签名只统计了选中数量、没有记录*哪些*行被
  选中，于是把一行换成另一行时，屏幕上仍留着旧的勾选。
- **Chrome 的自动填充不再在主题化的表单字段上留下浅黄色条**；两个应用都覆盖了
  `:-webkit-autofill` 以匹配表面色。
- **「设置」不再像第二个主题按钮。** 齿轮图标是「圆环加八条射线」，在 18px 下与太阳图标
  无法区分，而在 Webmail 顶栏里两者正好相邻。两个应用的「设置」现在都用滑块图标，屏幕上
  只剩主题切换一个太阳。


- **读完邮件会更新为已读。** `renderList` 只在列表的“形状”（条数、总数、选中项）变化时才
  重绘，而标志位变化不影响其中任何一项，于是 `PATCH /seen` 成功了，行上的未读圆点和侧栏
  徽标却纹丝不动。在阅读窗格里加星标也是同样的问题。
- **侧栏未读徽标不再永久滞后。** `folders.unseen_count` 是投递与移动会重算、而标志位变化
  不会重算的反规范化计数器，于是读完邮件后徽标会一直声称有未读，直到下一次投递。
- **省略 `</head>` 的 HTML 邮件能正常显示。** 净化器把“缺少闭合标签”当成“丢弃文档其余部分”，
  于是所有这样写的邮件正文都被清空——而 HTML 允许省略该标签——阅读窗格只会报“这封邮件没有
  正文”。
- **样式表不再变成邮件预览。** 摘要生成器只去标签、不去 `head`/`style`/`script`/`title` 的
  内容，因此把 CSS 放在 `<style>` 里的 HTML 邮件预览出来是
  `#outlook a { padding:0; } body { margin:0; …`。同时也会解码实体，`Fish &amp; chips`
  预览为 `Fish & chips`。
- **HTML 阅读窗格能显示完整正文。** 沙箱 iframe 的源是不透明源，父页面读不到它的内容高度；
  原先那段尝试读取的代码其实是空操作，窗格一直卡在 240px 下限。现在由 CSS 给出固定且宽裕的
  高度，超长内容在窗格内部滚动。
- **登录后邮件列表会加载。** 路由在没有会话时先应用过一次，第二次应用又因“同一路由”而短路，
  于是 URL 指向的文件夹一直空着，直到点了*另一个*文件夹。
- **没有地址的账号会说明原因**，而不是显示“还没有文件夹。”；它的“写邮件”按钮（以及 `c`
  快捷键）会禁用，而不是打开一个根本发不出去的表单。
- **Admin 管理后台不会再永久白屏。** 未捕获错误、模块导入失败或启动长时间不结束时，现在会显示
  登录面板并打印失败原因，另有启动看门狗；此前的表现是一片空白，与服务器从未应答无法区分。

### 带入 0.1.7 的已知问题

发 0.1.3 时发现的两件事至今未了：已发布的镜像说不清自己是哪个构建，八篇文档里也仍留着写于
各 crate 尚不存在之时的 `_(planned)_` 断言。它们现在记录在 [`TODO_zh.md`](TODO_zh.md) 的
「下一版（0.1.7）」一节；第一项的修法见
[README — 带入 0.1.6 的待办](README_zh.md#带入-017-的待办)。

## [0.1.3] — 2026-09-19

### 新增

- **两个前端都支持简体中文，与英文并存。** 英文是源语言，其文本**就是**查找键，因此没有
  译文的字符串会退化为英文而不是出错；`shared/locales/zh-CN.js` 收录 881 条。语言选择器
  在「设置」里，选择按浏览器记忆，并且会一并要求 API 用该语言返回报错。
- **API 的错误消息支持按 `Accept-Language` 本地化**（RFC 9110 的 q 值）。`code` 不随语言
  变化。消息按「种类」与「细节」两半分别翻译——正是这一点覆盖了 126 处 `format!` 调用点，
  而不必给每一处都穿一个消息键。
- **`shared/`：两个前端共用的模块只留一份。** 服务端把它挂在 `/shared`，因此
  `../shared/api.js` 从 `/main.js` 与从 `/admin/main.js` 解析到同一个 URL；
  `api.shared_dir` 指定该目录，镜像也把它打进去。
- `tools/serve-frontends.mjs`——按路由器的同样三个挂载提供 `web/`、`admin/`、`shared/`，
  用于不需要服务器与数据库的前端开发。
- `ferroma user create --disabled`，与 `POST /users` 传 `enabled: false` 对应。

### 变更

- `/auth/me` 返回完整的邮箱记录，而不再是精简地址摘要，客户端因此能读到每个地址的配额与
  当前用量。
- `GET /users` 带上每个账号的地址；`GET /audit` 把操作者解析成地址，而不只发一个 id。
- 向导被停用时，`GET /setup` 返回 `404`，而不再是 `200 {required:false}`——客户端此前无法
  区分「已停用」与「已有管理员」。提交的 `hostname` 与 `server.hostname` 不一致时返回
  `400` 并指出该改哪个配置项，且校验发生在任何写入之前。
- 未知的 `/api/v1` 路径、以及方法不匹配的已知路径，都返回 `docs/api.md` §1.3 承诺的错误
  信封。此前未知路径会落到 Webmail 的 SPA 回退，以 `200` 返回 `index.html`。
- Admin 管理后台新增筛选栏、可排序可多选的表格、详情抽屉与真正的分区布局；`hidden` 属性现在
  真的会隐藏它标记的内容。
- Webmail 的写信对话框恢复成可以发送与保存。

### 修复

- **外发邮件现在真的会做 DKIM 签名。** `[dkim]` 早已完整实现（密钥解析、规范化、签名），
  却在生产路径上没有任何调用者：即便启用了 DKIM、发布了选择器、密钥也在库里，所有外发
  邮件仍然没有签名。
- Webmail 把每封邮件都显示为未读、每个星标都藏起来：API 发的是规范的小写标志串
  （`seen flagged`，由 `docs/fcp.md` §4 冻结），而客户端在匹配 `\Seen`。现在标志也会以
  布尔字段一并下发。
- `INBOX` 从来匹配不上，因为它不带 `special_use`——RFC 6154 没有 `\Inbox`。现在按名字
  识别；标准文件夹用显示名呈现，而 `folder.name` 始终是 IMAP 数据，绝不翻译。
- `POST /users` 会静默丢弃 `enabled`，控制台里的「账号已启用」勾选框因此毫无作用；清空
  显示名会报告成功，却什么都没改。
- Admin 仪表盘五个卡片里有四个渲染出字符串 `undefined`。
- 修改密码时「当前密码错误」被当作会话过期而重试；附件下载把 `304 Not Modified` 当成失败；
  徽章颜色在标签被提前翻译后丢失。
- `doctor` 的 loopback 遮蔽测试断言的是 Windows 的 `SO_REUSEADDR` 语义，在任何 Linux 主机
  上都会失败。
- `ferroma-api` 的集成测试辅助用「先查后建」创建共享测试库，与该测试二进制里的其它测试
  互相竞争。
- `scripts/docker-publish.sh` 现在可执行——它自己的用法示例正是这么用的。

### 移除

- `backup/` 与 `.probe/`——从未纳入版本控制的本地杂物：一个所含提交早已在历史里的打包，
  以及上一轮审计用完即弃的验证程序。

## [0.1.2] — 2026-09-17

### 新增

- Admin 应用在 `/admin/` 提供服务，并带目录重定向，使其相对资源 URL 解析到 Admin 目录内
  而不是 Webmail 的目录。

### 修复

- 两个前端改为读取 API 实际发出的字段名，并补上此前一直空着的区域。
- 前端静态检查在文档所述的位置运行。

## [0.1.1] — 2026-09-17

### 修复

- 一处未定义的 `renderChrome` 引用，它导致 Webmail 完全无法启动。

## [0.1.0] — 2026-09-17

首个版本：同一个进程里的 SMTP、IMAP 与 HTTP API，底层是 PostgreSQL 元数据、存放邮件正文
的 Maildir 与内容寻址的附件存储，以及 Webmail、Admin 两个应用和官方客户端的核心与命令行。

### 新增

- 发信中继，服务于 IP 没有 PTR 记录的主机。
- Docker Hub 发布，由 `v*` 标签驱动。

### 变更

- 运维脚本可执行。

### 修复

- `scripts/deploy.sh` 在 PostgreSQL 不可达时快速失败，可自行置备容器化 PostgreSQL，能在没有
  `postgres` 数据库的集群上存活，并且会去问容器它的超级用户是谁，而不是靠假设。
- `ferroma database init` 能在不假定存在 `postgres` 数据库的前提下创建缺失的数据库。
- TLS 配置被带入 IMAP 配置；`FERROMA_PUBLIC_URL` 以服务端真正读取的名字传进去。
